spring:
  datasource:
    url: jdbc:h2:mem:architecture;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false
  flyway:
    enabled: false

app:
  jwt:
    secret: test-secret-key-with-at-least-thirty-two-bytes
    access-token-minutes: 60
